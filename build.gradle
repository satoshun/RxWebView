subprojects {
  buildscript {
    repositories {
      jcenter()
      google()
      gradlePluginPortal()
    }
  }

  repositories {
    jcenter()
    google()
  }

  group = GROUP
  version = VERSION_NAME
}

task clean(type: Delete) {
  delete rootProject.buildDir
}
